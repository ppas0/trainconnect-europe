# TrainConnect Europe – Backend Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an.
# ACHTUNG: .env niemals in Git committen!

# Pflichtfelder ──────────────────────────────────────────────
JWT_SECRET=dein-geheimer-schluessel-min-32-zeichen-lang
JWT_ALGORITHM=HS256
JWT_EXPIRE_MIN=10080

# CORS (mehrere Origins kommagetrennt, * erlaubt alle)
CORS_ORIGINS=*

# Externe APIs ───────────────────────────────────────────────
TRANSPORT_REST_BASE=https://v6.db.transport.rest
STRIPE_API_KEY=sk_test_...        # Optional – nur für echte Zahlungen

# Push-Benachrichtigungen ────────────────────────────────────
VAPID_PUBLIC_KEY=BBtqVXaeWnmt5FTRE1HhLwJB9SpPrYS-RwpSN1Z44b0umEcHLNIjlXVJdY-rYScyS7-nz-r-IyK1cdp_2a77UNk
VAPID_PRIVATE_KEY_FILE=/app/backend/vapid_private.pem
VAPID_CONTACT=mailto:admin@trainconnect.eu

# Optional ───────────────────────────────────────────────────
PORT=5000
NODE_ENV=development
FRONTEND_BUILD_DIR=      # Leer lassen = automatisch aus backend/public

# Datenbankpfad (Standard: backend/data/db.json)
DB_PATH=
